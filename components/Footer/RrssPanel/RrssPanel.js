import React from 'react';

const RrssPanel = ({data}) => {
  
  console.log('items:', data);

  return ( 
    <React.Fragment>
      {/* <p>Síguenos en redes sociales</p>
      {
        data[0].rrss.map((item) => (
          {item.icon}
        ))
      } */}
    </React.Fragment>
  );
}
 
export default RrssPanel;